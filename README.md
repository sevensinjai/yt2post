# TO DO
[ ] Add the following into the build script
```
- Related Package
		- `apk add --update --no-cache python3 && ln -sf python3 /usr/bin/python`
		- `apk add py3-pip`
		- `RUN apk add --no-cache ffmpeg`
		- `pip install openai langchain langchain-community pydub --break-system-packages`
		- `pip install ffmpeg-python --break-system-packages``
		- `pip install --upgrade yt-dlp --break-system-packages`
		- chmod a+rw tmp/youtube
```